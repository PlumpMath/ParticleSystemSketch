#pragma once

enum particleMode{
    NOISE = 0,
    TRIGO,
    PENDULUM,
    SNOW,
    FOUNTAINS,
    PARTICLE_MODE_REPEL,
    PARTICLE_MODE_NEAREST_POINTS,
    PARTICLE_MODE_NOISE
};

//aggiungere un modo dove cadono a zig zag, come mosse da gravity + pendolo